{
  "annotations": {
    "list": [
      {
        "builtIn": 1,
        "datasource": { "type": "grafana", "uid": "-- Grafana --" },
        "enable": true,
        "hide": true,
        "iconColor": "rgba(0, 211, 255, 1)",
        "name": "Annotations & Alerts",
        "type": "dashboard"
      }
    ]
  },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 1,
  "id": null,
  "links": [],
  "liveNow": false,
  "panels": [
    {
      "datasource": { "type": "postgres", "uid": "postgres" },
      "fieldConfig": {
        "defaults": {
          "thresholds": {
            "mode": "absolute",
            "steps": [
              { "color": "red", "value": null },
              { "color": "yellow", "value": 5 },
              { "color": "green", "value": 10 }
            ]
          }
        },
        "overrides": []
      },
      "gridPos": { "h": 4, "w": 4, "x": 0, "y": 0 },
      "id": 1,
      "options": {
        "colorMode": "value",
        "graphMode": "none",
        "reduceOptions": { "calcs": ["lastNotNull"], "fields": "", "values": false }
      },
      "targets": [
        {
          "datasource": { "type": "postgres", "uid": "postgres" },
          "editorMode": "code",
          "format": "table",
          "rawQuery": true,
          "rawSql": "SELECT count(*) AS \"Signals Generated\" FROM signal_predictions WHERE created_at >= now() - interval '24 hours';",
          "refId": "A"
        }
      ],
      "title": "Signals Generated (24h)",
      "type": "stat"
    },
    {
      "datasource": { "type": "postgres", "uid": "postgres" },
      "fieldConfig": {
        "defaults": {
          "thresholds": {
            "mode": "absolute",
            "steps": [
              { "color": "red", "value": null },
              { "color": "yellow", "value": 3 },
              { "color": "green", "value": 5 }
            ]
          }
        },
        "overrides": []
      },
      "gridPos": { "h": 4, "w": 4, "x": 4, "y": 0 },
      "id": 2,
      "options": {
        "colorMode": "value",
        "graphMode": "none",
        "reduceOptions": { "calcs": ["lastNotNull"], "fields": "", "values": false }
      },
      "targets": [
        {
          "datasource": { "type": "postgres", "uid": "postgres" },
          "editorMode": "code",
          "format": "table",
          "rawQuery": true,
          "rawSql": "SELECT count(*) AS \"Signals Delivered\" FROM signal_predictions WHERE pipeline_stage = 'delivered' AND created_at >= now() - interval '24 hours';",
          "refId": "A"
        }
      ],
      "title": "Signals Delivered (24h)",
      "type": "stat"
    },
    {
      "datasource": { "type": "postgres", "uid": "postgres" },
      "fieldConfig": {
        "defaults": {
          "unit": "percentunit",
          "thresholds": {
            "mode": "absolute",
            "steps": [
              { "color": "red", "value": null },
              { "color": "yellow", "value": 0.45 },
              { "color": "green", "value": 0.55 }
            ]
          }
        },
        "overrides": []
      },
      "gridPos": { "h": 4, "w": 4, "x": 8, "y": 0 },
      "id": 3,
      "options": {
        "colorMode": "value",
        "graphMode": "none",
        "reduceOptions": { "calcs": ["lastNotNull"], "fields": "", "values": false }
      },
      "targets": [
        {
          "datasource": { "type": "postgres", "uid": "postgres" },
          "editorMode": "code",
          "format": "table",
          "rawQuery": true,
          "rawSql": "SELECT avg(CASE WHEN outcome_label = 'win' THEN 1.0 ELSE 0.0 END) AS \"Win Rate (7d)\" FROM signal_predictions WHERE resolved_at IS NOT NULL AND pipeline_stage = 'delivered' AND created_at >= now() - interval '7 days';",
          "refId": "A"
        }
      ],
      "title": "Win Rate (7d, delivered)",
      "type": "stat"
    },
    {
      "datasource": { "type": "postgres", "uid": "postgres" },
      "fieldConfig": {
        "defaults": {
          "unit": "percentunit",
          "thresholds": {
            "mode": "absolute",
            "steps": [
              { "color": "red", "value": null },
              { "color": "yellow", "value": 0 },
              { "color": "green", "value": 0.001 }
            ]
          }
        },
        "overrides": []
      },
      "gridPos": { "h": 4, "w": 4, "x": 12, "y": 0 },
      "id": 4,
      "options": {
        "colorMode": "value",
        "graphMode": "none",
        "reduceOptions": { "calcs": ["lastNotNull"], "fields": "", "values": false }
      },
      "targets": [
        {
          "datasource": { "type": "postgres", "uid": "postgres" },
          "editorMode": "code",
          "format": "table",
          "rawQuery": true,
          "rawSql": "SELECT avg(return_pct) AS \"Avg Return (7d)\" FROM signal_predictions WHERE resolved_at IS NOT NULL AND pipeline_stage = 'delivered' AND created_at >= now() - interval '7 days';",
          "refId": "A"
        }
      ],
      "title": "Avg Return (7d, delivered)",
      "type": "stat"
    },
    {
      "datasource": { "type": "postgres", "uid": "postgres" },
      "fieldConfig": {
        "defaults": {
          "unit": "percentunit",
          "thresholds": {
            "mode": "absolute",
            "steps": [
              { "color": "red", "value": null },
              { "color": "yellow", "value": 0 },
              { "color": "green", "value": 0.001 }
            ]
          }
        },
        "overrides": []
      },
      "gridPos": { "h": 4, "w": 4, "x": 16, "y": 0 },
      "id": 5,
      "options": {
        "colorMode": "value",
        "graphMode": "none",
        "reduceOptions": { "calcs": ["lastNotNull"], "fields": "", "values": false }
      },
      "targets": [
        {
          "datasource": { "type": "postgres", "uid": "postgres" },
          "editorMode": "code",
          "format": "table",
          "rawQuery": true,
          "rawSql": "SELECT avg(return_pct) AS \"EV (all time)\" FROM signal_predictions WHERE resolved_at IS NOT NULL AND pipeline_stage = 'delivered';",
          "refId": "A"
        }
      ],
      "title": "EV (all time, delivered)",
      "type": "stat"
    },
    {
      "datasource": { "type": "postgres", "uid": "postgres" },
      "fieldConfig": {
        "defaults": {
          "unit": "percentunit",
          "thresholds": {
            "mode": "absolute",
            "steps": [
              { "color": "red", "value": null },
              { "color": "yellow", "value": 0.45 },
              { "color": "green", "value": 0.55 }
            ]
          }
        },
        "overrides": []
      },
      "gridPos": { "h": 4, "w": 4, "x": 20, "y": 0 },
      "id": 6,
      "options": {
        "colorMode": "value",
        "graphMode": "none",
        "reduceOptions": { "calcs": ["lastNotNull"], "fields": "", "values": false }
      },
      "targets": [
        {
          "datasource": { "type": "postgres", "uid": "postgres" },
          "editorMode": "code",
          "format": "table",
          "rawQuery": true,
          "rawSql": "SELECT avg(CASE WHEN outcome_label = 'win' THEN 1.0 ELSE 0.0 END) AS \"Win Rate (24h)\" FROM signal_predictions WHERE resolved_at IS NOT NULL AND pipeline_stage = 'delivered' AND created_at >= now() - interval '24 hours';",
          "refId": "A"
        }
      ],
      "title": "Win Rate (24h, delivered)",
      "type": "stat"
    },
    {
      "datasource": { "type": "postgres", "uid": "postgres" },
      "fieldConfig": { "defaults": {}, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 4 },
      "id": 7,
      "options": {
        "cellHeight": "sm",
        "footer": { "countRows": false, "fields": "", "reducer": ["sum"], "show": false },
        "showHeader": true,
        "sortBy": [{ "displayName": "ev", "desc": true }]
      },
      "targets": [
        {
          "datasource": { "type": "postgres", "uid": "postgres" },
          "editorMode": "code",
          "format": "table",
          "rawQuery": true,
          "rawSql": "SELECT source_channel, count(*) AS signals, count(*) FILTER (WHERE resolved_at IS NOT NULL) AS resolved, round(avg(CASE WHEN outcome_label = 'win' THEN 1.0 ELSE 0.0 END) FILTER (WHERE resolved_at IS NOT NULL), 3) AS win_rate, round(avg(return_pct) FILTER (WHERE resolved_at IS NOT NULL), 5) AS ev FROM signal_predictions WHERE pipeline_stage = 'delivered' AND source_channel IS NOT NULL GROUP BY source_channel HAVING count(*) FILTER (WHERE resolved_at IS NOT NULL) >= 5 ORDER BY ev DESC;",
          "refId": "A"
        }
      ],
      "title": "Top Sources by EV (delivered, min 5 resolved)",
      "type": "table"
    },
    {
      "datasource": { "type": "postgres", "uid": "postgres" },
      "fieldConfig": { "defaults": {}, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 4 },
      "id": 8,
      "options": {
        "cellHeight": "sm",
        "footer": { "countRows": false, "fields": "", "reducer": ["sum"], "show": false },
        "showHeader": true,
        "sortBy": [{ "displayName": "ev", "desc": true }]
      },
      "targets": [
        {
          "datasource": { "type": "postgres", "uid": "postgres" },
          "editorMode": "code",
          "format": "table",
          "rawQuery": true,
          "rawSql": "SELECT ticker, count(*) AS signals, count(*) FILTER (WHERE resolved_at IS NOT NULL) AS resolved, round(avg(CASE WHEN outcome_label = 'win' THEN 1.0 ELSE 0.0 END) FILTER (WHERE resolved_at IS NOT NULL), 3) AS win_rate, round(avg(return_pct) FILTER (WHERE resolved_at IS NOT NULL), 5) AS ev FROM signal_predictions WHERE pipeline_stage = 'delivered' GROUP BY ticker HAVING count(*) FILTER (WHERE resolved_at IS NOT NULL) >= 5 ORDER BY ev DESC;",
          "refId": "A"
        }
      ],
      "title": "Top Tickers by EV (delivered, min 5 resolved)",
      "type": "table"
    },
    {
      "collapsed": false,
      "gridPos": { "h": 1, "w": 24, "x": 0, "y": 12 },
      "id": 20,
      "title": "Shadow Experiments",
      "type": "row"
    },
    {
      "datasource": { "type": "postgres", "uid": "postgres" },
      "fieldConfig": { "defaults": {}, "overrides": [] },
      "gridPos": { "h": 7, "w": 8, "x": 0, "y": 13 },
      "id": 9,
      "options": {
        "cellHeight": "sm",
        "footer": { "countRows": false, "fields": "", "reducer": ["sum"], "show": false },
        "showHeader": true
      },
      "targets": [
        {
          "datasource": { "type": "postgres", "uid": "postgres" },
          "editorMode": "code",
          "format": "table",
          "rawQuery": true,
          "rawSql": "SELECT ai_gate_decision, count(*) AS signals, round(avg(CASE WHEN outcome_label = 'win' THEN 1.0 ELSE 0.0 END), 3) AS win_rate, round(avg(return_pct), 5) AS ev FROM signal_predictions WHERE resolved_at IS NOT NULL AND ai_gate_decision IS NOT NULL AND pipeline_stage = 'delivered' GROUP BY ai_gate_decision ORDER BY ai_gate_decision;",
          "refId": "A"
        }
      ],
      "title": "AI Gating Impact (shadow)",
      "type": "table"
    },
    {
      "datasource": { "type": "postgres", "uid": "postgres" },
      "fieldConfig": { "defaults": {}, "overrides": [] },
      "gridPos": { "h": 7, "w": 8, "x": 8, "y": 13 },
      "id": 10,
      "options": {
        "cellHeight": "sm",
        "footer": { "countRows": false, "fields": "", "reducer": ["sum"], "show": false },
        "showHeader": true
      },
      "targets": [
        {
          "datasource": { "type": "postgres", "uid": "postgres" },
          "editorMode": "code",
          "format": "table",
          "rawQuery": true,
          "rawSql": "SELECT CASE WHEN source_weight IS NULL THEN 'no_weight' WHEN source_weight >= 1.1 THEN 'strong (>=1.1)' WHEN source_weight >= 1.0 THEN 'neutral (1.0-1.09)' ELSE 'weak (<1.0)' END AS weight_tier, count(*) AS signals, round(avg(CASE WHEN outcome_label = 'win' THEN 1.0 ELSE 0.0 END), 3) AS win_rate, round(avg(return_pct), 5) AS ev FROM signal_predictions WHERE resolved_at IS NOT NULL AND pipeline_stage = 'delivered' GROUP BY 1 ORDER BY 1;",
          "refId": "A"
        }
      ],
      "title": "Source Weight Tiers (shadow)",
      "type": "table"
    },
    {
      "datasource": { "type": "postgres", "uid": "postgres" },
      "fieldConfig": { "defaults": {}, "overrides": [] },
      "gridPos": { "h": 7, "w": 8, "x": 16, "y": 13 },
      "id": 11,
      "options": {
        "cellHeight": "sm",
        "footer": { "countRows": false, "fields": "", "reducer": ["sum"], "show": false },
        "showHeader": true
      },
      "targets": [
        {
          "datasource": { "type": "postgres", "uid": "postgres" },
          "editorMode": "code",
          "format": "table",
          "rawQuery": true,
          "rawSql": "SELECT COALESCE(global_alignment, 'not_enriched') AS alignment, count(*) AS signals, round(avg(CASE WHEN outcome_label = 'win' THEN 1.0 ELSE 0.0 END), 3) AS win_rate, round(avg(return_pct), 5) AS ev FROM signal_predictions WHERE resolved_at IS NOT NULL AND pipeline_stage = 'delivered' GROUP BY 1 ORDER BY 1;",
          "refId": "A"
        }
      ],
      "title": "Global Alignment Impact (shadow)",
      "type": "table"
    }
  ],
  "refresh": "1m",
  "schemaVersion": 39,
  "tags": ["tinvest", "operator", "overview"],
  "templating": { "list": [] },
  "time": { "from": "now-7d", "to": "now" },
  "timepicker": {},
  "timezone": "",
  "title": "Operator Overview",
  "uid": "operator-overview",
  "version": 1,
  "weekStart": ""
}
