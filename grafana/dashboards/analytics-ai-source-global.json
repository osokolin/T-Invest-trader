{
  "annotations": {
    "list": [
      {
        "builtIn": 1,
        "datasource": { "type": "grafana", "uid": "-- Grafana --" },
        "enable": true,
        "hide": true,
        "iconColor": "rgba(0, 211, 255, 1)",
        "name": "Annotations & Alerts",
        "type": "dashboard"
      }
    ]
  },
  "description": "Research view for source attribution, AI shadow decisions, and global-context alignment. Use resolved sample sizes before drawing conclusions.",
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 1,
  "id": null,
  "links": [{ "asDropdown": true, "includeVars": true, "keepTime": true, "tags": ["tinvest"], "targetBlank": false, "title": "T-Invest dashboards", "tooltip": "Open another T-Invest dashboard", "type": "dashboards" }],
  "liveNow": false,
  "panels": [
    {
      "collapsed": false,
      "gridPos": { "h": 1, "w": 24, "x": 0, "y": 0 },
      "id": 50,
      "title": "Telegram Source Attribution",
      "type": "row"
    },
    {
      "datasource": { "type": "postgres", "uid": "postgres" },
      "fieldConfig": { "defaults": {}, "overrides": [] },
      "gridPos": { "h": 9, "w": 24, "x": 0, "y": 1 },
      "id": 1,
      "options": {
        "cellHeight": "sm",
        "footer": { "countRows": false, "fields": "", "reducer": ["sum"], "show": false },
        "showHeader": true,
        "sortBy": [{ "displayName": "ev", "desc": true }]
      },
      "targets": [
        {
          "datasource": { "type": "postgres", "uid": "postgres" },
          "editorMode": "code",
          "format": "table",
          "rawQuery": true,
          "rawSql": "SELECT source_channel, count(*) AS total, count(*) FILTER (WHERE pipeline_stage = 'delivered') AS delivered, count(*) FILTER (WHERE resolved_at IS NOT NULL) AS resolved, round(avg(CASE WHEN outcome_label = 'win' THEN 1.0 ELSE 0.0 END) FILTER (WHERE resolved_at IS NOT NULL)::numeric, 3) AS win_rate, round(avg(return_pct) FILTER (WHERE resolved_at IS NOT NULL)::numeric, 5) AS avg_return, round(avg(return_pct) FILTER (WHERE resolved_at IS NOT NULL AND pipeline_stage = 'delivered')::numeric, 5) AS delivered_avg_return, round(avg(source_weight) FILTER (WHERE source_weight IS NOT NULL)::numeric, 3) AS avg_source_weight FROM signal_predictions WHERE source_channel IS NOT NULL GROUP BY source_channel HAVING count(*) >= 3 ORDER BY total DESC;",
          "refId": "A"
        }
      ],
      "title": "Telegram Source Performance · Min 3 Signals",
      "type": "table"
    },
    {
      "collapsed": false,
      "gridPos": { "h": 1, "w": 24, "x": 0, "y": 10 },
      "id": 51,
      "title": "AI Agreement & Divergence",
      "type": "row"
    },
    {
      "datasource": { "type": "postgres", "uid": "postgres" },
      "fieldConfig": {
        "defaults": {
          "color": { "mode": "palette-classic" },
          "thresholds": {
            "mode": "absolute",
            "steps": [{ "color": "green", "value": null }]
          }
        },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 8, "x": 0, "y": 11 },
      "id": 5,
      "options": {
        "displayMode": "gradient",
        "minVizHeight": 10,
        "minVizWidth": 0,
        "namePlacement": "auto",
        "orientation": "horizontal",
        "reduceOptions": { "calcs": ["lastNotNull"], "fields": "", "values": false },
        "showUnfilled": true,
        "valueMode": "color"
      },
      "targets": [
        {
          "datasource": { "type": "postgres", "uid": "postgres" },
          "editorMode": "code",
          "format": "table",
          "rawQuery": true,
          "rawSql": "SELECT COALESCE(a.divergence_bucket, 'no_analysis') AS bucket, count(*) AS count FROM signal_predictions sp LEFT JOIN signal_ai_analyses a ON a.signal_id = sp.id WHERE sp.pipeline_stage = 'delivered' GROUP BY 1 ORDER BY count DESC;",
          "refId": "A"
        }
      ],
      "title": "AI Divergence Buckets (delivered)",
      "type": "bargauge"
    },
    {
      "datasource": { "type": "postgres", "uid": "postgres" },
      "fieldConfig": { "defaults": {}, "overrides": [] },
      "gridPos": { "h": 8, "w": 16, "x": 8, "y": 11 },
      "id": 6,
      "options": {
        "cellHeight": "sm",
        "footer": { "countRows": false, "fields": "", "reducer": ["sum"], "show": false },
        "showHeader": true,
        "sortBy": [{ "displayName": "ev", "desc": true }]
      },
      "targets": [
        {
          "datasource": { "type": "postgres", "uid": "postgres" },
          "editorMode": "code",
          "format": "table",
          "rawQuery": true,
          "rawSql": "SELECT COALESCE(a.divergence_bucket, 'no_analysis') AS bucket, count(*) AS signals, round(avg(CASE WHEN sp.outcome_label = 'win' THEN 1.0 ELSE 0.0 END)::numeric, 3) AS win_rate, round(avg(sp.return_pct)::numeric, 5) AS avg_return FROM signal_predictions sp LEFT JOIN signal_ai_analyses a ON a.signal_id = sp.id WHERE sp.resolved_at IS NOT NULL AND sp.pipeline_stage = 'delivered' GROUP BY 1 HAVING count(*) >= 3 ORDER BY signals DESC;",
          "refId": "A"
        }
      ],
      "title": "Outcome by AI Divergence · Min 3 Resolved",
      "type": "table"
    },
    {
      "collapsed": false,
      "gridPos": { "h": 1, "w": 24, "x": 0, "y": 19 },
      "id": 52,
      "title": "AI Gate · Shadow Only",
      "type": "row"
    },
    {
      "datasource": { "type": "postgres", "uid": "postgres" },
      "fieldConfig": { "defaults": {}, "overrides": [] },
      "gridPos": { "h": 7, "w": 24, "x": 0, "y": 20 },
      "id": 8,
      "options": {
        "cellHeight": "sm",
        "footer": { "countRows": false, "fields": "", "reducer": ["sum"], "show": false },
        "showHeader": true
      },
      "targets": [
        {
          "datasource": { "type": "postgres", "uid": "postgres" },
          "editorMode": "code",
          "format": "table",
          "rawQuery": true,
          "rawSql": "SELECT COALESCE(ai_gate_decision, 'no_gate') AS gate_decision, count(*) AS signals, count(*) FILTER (WHERE resolved_at IS NOT NULL) AS resolved, round(avg(CASE WHEN outcome_label = 'win' THEN 1.0 ELSE 0.0 END) FILTER (WHERE resolved_at IS NOT NULL)::numeric, 3) AS win_rate, round(avg(return_pct) FILTER (WHERE resolved_at IS NOT NULL)::numeric, 5) AS avg_return, round(avg(confidence) FILTER (WHERE resolved_at IS NOT NULL)::numeric, 4) AS avg_confidence FROM signal_predictions WHERE pipeline_stage = 'delivered' GROUP BY 1 ORDER BY 1;",
          "refId": "A"
        }
      ],
      "title": "AI Gate Outcome Comparison · Delivered Signals",
      "type": "table"
    },
    {
      "collapsed": false,
      "gridPos": { "h": 1, "w": 24, "x": 0, "y": 27 },
      "id": 53,
      "title": "Global Context Alignment · Shadow Only",
      "type": "row"
    },
    {
      "datasource": { "type": "postgres", "uid": "postgres" },
      "fieldConfig": { "defaults": {}, "overrides": [] },
      "gridPos": { "h": 7, "w": 24, "x": 0, "y": 28 },
      "id": 10,
      "options": {
        "cellHeight": "sm",
        "footer": { "countRows": false, "fields": "", "reducer": ["sum"], "show": false },
        "showHeader": true
      },
      "targets": [
        {
          "datasource": { "type": "postgres", "uid": "postgres" },
          "editorMode": "code",
          "format": "table",
          "rawQuery": true,
          "rawSql": "SELECT COALESCE(global_alignment, 'not_enriched') AS alignment, count(*) AS signals, count(*) FILTER (WHERE resolved_at IS NOT NULL) AS resolved, round(avg(CASE WHEN outcome_label = 'win' THEN 1.0 ELSE 0.0 END) FILTER (WHERE resolved_at IS NOT NULL)::numeric, 3) AS win_rate, round(avg(return_pct) FILTER (WHERE resolved_at IS NOT NULL)::numeric, 5) AS avg_return, round(avg(global_adjustment) FILTER (WHERE global_adjustment IS NOT NULL)::numeric, 4) AS avg_adjustment FROM signal_predictions WHERE pipeline_stage = 'delivered' GROUP BY 1 ORDER BY 1;",
          "refId": "A"
        }
      ],
      "title": "Outcome by Global Alignment · Delivered Signals",
      "type": "table"
    }
  ],
  "refresh": "5m",
  "schemaVersion": 39,
  "tags": ["tinvest", "analytics", "ai", "source", "global"],
  "templating": { "list": [] },
  "time": { "from": "now-30d", "to": "now" },
  "timepicker": {},
  "timezone": "",
  "title": "Signal Research · Sources, AI & Global Context",
  "uid": "analytics-ai-source-global",
  "version": 1,
  "weekStart": ""
}
